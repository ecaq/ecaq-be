@* Inherits from the original InputText component *@
@inherits InputText

@*
<input @attributes="AdditionalAttributes" 
       class="block w-full rounded-md border-gray-300 pl-10 sm:text-sm"
       type="password"
       @bind="CurrentValueAsString" 
       @bind:event="oninput" />*@

<input @attributes="InputAttributes"
       class="block w-full border-0 py-2.5 pl-7 text-appBlack bg-beeBackground focus:ring-0 text-sm"
       id="pwd"
       @bind="CurrentValueAsString"
       @bind:event="oninput" />

@code {
    [Parameter]
    public Dictionary<string, object> InputAttributes { get; set; }
}
